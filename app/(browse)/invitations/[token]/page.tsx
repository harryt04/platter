import { PlaceholderPage } from '@/components/states/placeholder-page'
export default async function InvitationPage() {
  return (
    <PlaceholderPage
      title="List invitation"
      description="This invitation summary is ready for authentication and connectivity checks. Accepting invitations arrives with the Lists feature."
      action="Sign in"
      actionHref="/sign-in"
    />
  )
}
